{
  "name": "id-extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "chrome-extension run ./manifest.json -o ./build",
    "build": "chrome-extension build ./manifest.json -o ./build"
  },
  "author": "Parity Technologies <info@ethcore.io>",
  "license": "GPL3",
  "devDependencies": {
    "chrome-extension-scripts": "^0.9.5"
  }
}
